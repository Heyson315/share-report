# Copilot Tools Toolbox - Environment Configuration Example
#
# This file demonstrates optional environment variables for the copilot toolbox.
# Copy this file to .env if you want to customize behavior.
#
# IMPORTANT: Never commit actual secrets or sensitive data to .env files!
# The .env file should be in .gitignore

# ============================================================================
# Copilot Toolbox Settings
# ============================================================================

# Enable verbose logging (adds non-sensitive metadata to check-workspace output)
# Values: true, false
# Default: false
COPILOT_TOOLBOX_VERBOSE=false

# ============================================================================
# Security Notes
# ============================================================================
#
# The copilot toolbox is designed to be secure by default:
# - Never logs environment variable values
# - Skips .env files during scanning
# - Only reads files within repository boundaries
# - No network calls or external dependencies
#
# Safe to set:
# - Feature flags (VERBOSE, DEBUG)
# - Non-sensitive configuration (LOG_LEVEL, OUTPUT_FORMAT)
#
# NEVER set in .env:
# - API keys or tokens
# - Passwords or credentials
# - Personal information
# - Production secrets
#
# For sensitive credentials, use:
# - GitHub Secrets (in CI/CD)
# - System keyring
# - Secure credential managers
# - Environment-specific configuration (not committed to git)
